#include<iostream>
using namespace std;
class Multiplication
{ public:
      int num1,num2,res;
      public:
      void input()
      {
          cout<<"Enter The Number:";
          cin>>num1>>num2;
      }
      void calculator() {
        res = num1*num2;
      }
      void output() {
        cout<< "The Multiplication is "<<num1<<" and "<<num2<<" is "<<res<< endl;
      }
};
      int main(){
        Multiplication mul;
        mul.input();
        mul.calculator();
        mul.output();
        return 0;
      }
